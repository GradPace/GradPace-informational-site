[project]
name = "prodev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.2",
    "flask-caching>=2.4.1",
    "flask-compress>=1.24",
]

[dependency-groups]
prod = [
    "gunicorn>=26.1.0",
]
